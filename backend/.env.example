# Authentication Secrets
# Generate secure values with: openssl rand -base64 32
JWT_SECRET=replace-with-secure-random-string-at-least-32-chars
JWT_REFRESH_SECRET=replace-with-different-secure-random-string

# Server Configuration (optional)
# PORT=3000
# HOST=0.0.0.0
